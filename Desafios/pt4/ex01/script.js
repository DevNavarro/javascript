function contar() {
    var ini = window.document.querySelector('input#txtinicio')
    var fi = window.document.querySelector('input#txtfim')
    var pas = window.document.querySelector('input#txtpasso')
    var res = window.document.querySelector('div#res')
    var inicio = Number('ini.value')
    var fim = Number('fi.value')
    var passo = Number('pas.value')


    for (var inicio; index < array.length; index++) {
        
    }
}